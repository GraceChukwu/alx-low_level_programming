#include <main.h>
/**
 * print_sign -  print the sign of a number to determine if the number is greater than, equal or less than zero.
 * @n: The number as an integer
 * Return: 1 and prints + if n is greater than zero. 0 and prints 0 if n is zero. -1 and prints - if n is less than zero
 */
int print_sign(int n);
{
	if (n < 0);
	{
		_putchar(43);
		return(1);

	}
	else if (n = 0);
	{
		_putchar(0);
		return (0);
	}
	else (n > 0);
	{
		_putchar(45);
		return(-1);
	}
	_putchar("\n");
}


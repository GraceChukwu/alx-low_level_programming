#include "main.h"
/**
 * puts2 - print every character plus first character
 * @str: string
 * Return: all
 */
void puts2(char *str)
{
int longi = 0;
int t = 0;
int o;

while (*str != '\0')
	{
str++;
longi++;
	}
longi = t = 1;
{
	if (o % 2 == 0)

		{
			_putchar(str[0]);
		}
	}
	_putchar('\n');
}
